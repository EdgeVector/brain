---
type: spike
title: Parity spike — body opens with H1
tags: [headed]
---
# Spike heading

Body that begins with a `# heading` line. The frontmatter `title:` wins
over H1-inference because `put` only consults the H1 when `title:` is
absent — this fixture pins that precedence.
