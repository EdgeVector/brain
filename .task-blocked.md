BLOCKED: 121 live non-tombstoned records remain in the legacy FbrainKindNote schema on the local daemon — consolidation has not been run here.

The prompt's STEP 1 is explicit: "Verify prerequisite: against prod schema service, query the legacy noteSchema canonical hash and assert no non-tombstoned rows remain. If any remain, file a blocker (`.task-blocked.md`) — the consolidation didn't fully cover them."

What's needed to unblock:
- Run `fbrain consolidate` against the daemon at http://127.0.0.1:9001 (the team's source-of-truth daemon, per `~/.fbrain/config.json`) so its 121 live legacy rows are migrated into the per-kind schemas and tombstoned in noteSchema.
- Prereq for that: the installed CLI is 0.0.1 and the config is `configVersion: 2` (pre-Phase-E shape). The user needs to (a) install the post-#63 fbrain CLI, (b) `fbrain init` to pick up per-kind canonicals + the `__legacy_note__` hash, (c) `fbrain consolidate` to migrate the rows.
- Re-verify by re-running the query below and confirming `live by kind: {}`.
- Then resume this task.

Diagnostic:
```
$ curl -sS -X POST http://127.0.0.1:9001/api/query \
    -H "Content-Type: application/json" \
    -H "X-User-Hash: dcf41c3a2a0a8afe33477d5422a84159" \
    -d '{"schema_name":"88694ea86b303c03d00b91516c27cdb610de80d4706b67dc45e6d58372931ebc","fields":["slug","kind","tags"]}'
{"total_count":176, "returned_count":100, "has_more":true, ...}

# Aggregating across both pages:
total_count: 176
live (non-tombstoned): 121
  concept: 87, project: 18, reference: 5, preference: 6, agent: 3, spike: 2
tombstoned: 55
```

Schema hash 88694ea86b303c03d00b91516c27cdb610de80d4706b67dc45e6d58372931ebc verified via schema service as `FbrainKindNote` (10 fields including v1_marker_a/b).

Local state observed (relevant context — not a bug):
- `~/.fbrain/config.json` is `configVersion: 2`, all six Phase 6 kinds point to the legacy `88694ea…` hash (pre-Phase-E shape — never re-init'd post-#62 or #63).
- The installed `fbrain` binary at `~/.bun/bin/fbrain` reports `fbrain 0.0.1`. New Phase 6 writes are still landing in noteSchema on this daemon because of the stale config + binary.

Why this matters for this cleanup PR:
- After this PR removes the legacy read fallback, those 121 records become unreadable from fbrain. The new auto-heal migration re-runs `fbrain init` but does not migrate records — only consolidate does that.
- Merging this PR without consolidating first orphans the data on the team's daemon.

If "prod" in the prompt meant a different daemon (one I don't have access to) and consolidation has been run there, please leave a comment or unblock the kanban task with a confirmation; I'll resume immediately.
